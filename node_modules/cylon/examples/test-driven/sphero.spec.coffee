'use strict';

source "sphero"

Logger.setup(false) # quiet logger for tests

describe "Sphero", ->
  robot = new Cylon.Robot
    name: "irobot"
    work: testWork

  it "has work to do every 3 seconds"
  it "receives collision events"
  it "must roll every 3 seconds"
